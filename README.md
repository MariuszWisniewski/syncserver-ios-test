syncserver-ios-test
===================

Example app using Syncano Sync Server

Project uses [CocoaPods](http://cocoapods.org/), but it was uploaded in a way that you don't need to configure anything to use it.

Just add your API Key, instance domain name, and project and collection id.
